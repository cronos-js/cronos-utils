{
  "name": "cronos-utils",
  "version": "0.1.6",
  "description": "",
  "main": "./bin/cronos-build.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:build": "esbuild ./cronos-build/src/index.js --bundle --outfile=./bin/cronos-build.js --platform=node",
    "build:preview": "esbuild ./cronos-preview/src/index.js --bundle --outfile=./bin/cronos-preview.js --platform=node",
    "build:watch": "esbuild ./cronos-watch/src/index.js --bundle --outfile=./bin/cronos-watch.js --platform=node",
    "build": "npm run build:build && npm run build:preview && npm run build:watch"
  },
  "files": [
    "bin"
  ],
  "bin": {
    "cronos-build": "./bin/cronos-build.js",
    "cronos-preview": "./bin/cronos-preview.js",
    "cronos-watch": "./bin/cronos-watch.js"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@swc/cli": "0.1.62",
    "esbuild": "0.19.5"
  },
  "dependencies": {
    "express": "^4.18.2"
  }
}